[pytest]
markers =
    unit: unit tests
    security: security-related checks
    accessibility: accessibility tests (requires Selenium + axe)
    performance: performance tests
    slow: slower-running test cases
    integration: integration test suite
    api: API-level checks
    database: database-heavy tests
